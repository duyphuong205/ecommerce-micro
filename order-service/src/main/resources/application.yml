server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/order-micro
    username: postgres
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  application:
    name: order-service

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/